---
import { storyblokEditable } from '@storyblok/astro';
const { blok } = Astro.props;
---

<article {...storyblokEditable(blok)} class="about__container">
  <h3 class="about__title">{blok.title}</h3>
  <p>{blok.text}</p>
</article>
